# Backup before revert (2026-05-25)

This folder contains the **admin + SQL + CSV/XLSX upload** version before restoring the simpler portal UI.

To restore the admin version, copy these files back:

- `inventory.js`, `tasks.js`, `inventory.html`, `tasks.html`, `server.js`
- Also restore from git if you initialize a repo later.

The live project was reverted to the **simpler employee portal** (no admin panels on inventory/tasks pages).
